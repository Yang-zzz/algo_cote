// 선택 정렬
// 배열에서 가장 작은 값 (or 큰 값)을 선택해 정해진 위치(인덱스0)로 보내는 정렬
// part1. 배열에서 최솟값을 찾기

const array = [5, 13, 3, 20, 9, 16, 1, 4];

function select(arr) {
    let min = 0;
    for(let i=0; i<arr.length; i++) {
        for(let j=i+1; j<arr.length j++) {
            if(arr[i] > arr[j]) {}
        }
    }
}

select(arr);
